[package]
name = "bio-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Zero-dependency core for parsing and analysing DNA/RNA sequences"

[lib]
crate-type = ["rlib"]
